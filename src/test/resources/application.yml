spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fit_world_test
    username: postgres
    password: penon.veserti
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    showSql: true
    hibernate:
      ddl-auto: create
      ddlAuto: create
